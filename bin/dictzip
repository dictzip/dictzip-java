#!/bin/bash
# readlink follows any symbolic links to get the real file
APPPATH=`dirname "$(readlink -nf $0)"`
java -cp "${APPPATH}/java-getopt-1.1.0.jar,${APPPATH}/dictzip-cli.jar" org.dict.zip.Main $*
