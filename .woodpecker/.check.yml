when:
  - event: pull_request
  - event: tag
    ref: refs/tags/v*
  - event: push
    branch: [master, releases/*]

clone:
  git:
    image: woodpeckerci/plugin-git
    settings:
      partial: false
      depth: 1
      lfs: false
      tags: true

steps:
  build:
    image: adoptopenjdk/openjdk11:alpine
    commands:
      - sudo apk add dictzip
      - ./gradlew --no-daemon clean check
